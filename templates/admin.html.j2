{% extends 'layout.html.j2' %}

{% block title %}Administration{% endblock %}

{% block body %}
    <h1 class="text-center mt-5 mb-5">Administration</h1>

    <div class="container">
        <div class="row">

            <a class="btn btn-success mb-4" href="{{ url_for('add') }}" role="button">Ajouter un site</a>

            <table class="table col-sm-12">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Site</th>
                        <th scope="col">Statut</th>
                        <th scope="col">Gérer</th>
                    </tr>
                </thead>
                <tbody>
                    {% for site in sites %}
                        <tr>
                            <th scope="row">{{ loop.index }}</th>
                            <td><a href="{{ site[1] }}" target="_blank">{{ site[1] }}</a></td>
                            <td class="d-flex {% if status[loop.index0] == 200 %}text-success{% else %}text-danger{% endif %}">
                                {{ status[loop.index0] }}
                                {% if status[loop.index0] == 200 %}
                                    <i class="material-icons ml-3">done</i>
                                {% else %}
                                    <i class="material-icons ml-3">close</i>
                                {% endif %}
                            </td>
                            <td><a class="btn btn-primary" href="{{ url_for('history', id=site[0]) }}" role="button">Voir l'historique</a>
                            <a class="btn btn-warning" href="{{ url_for('edit', id=site[0]) }}" role="button">Modifier un site</a>
                            <a class="btn btn-danger" href="{{ url_for('delete', id=site[0]) }}" role="button">Supprimer un site</a></td>
                        </tr>
                    {% endfor %}

                </tbody>
            </table>
        </div>
    </div>

{% endblock %}
